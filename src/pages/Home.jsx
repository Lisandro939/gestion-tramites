import React, { useState } from 'react';
import styled from 'styled-components';

const Home = () => {

  return (
    <div>
        <Title>Home</Title>
    </div>
  );
};

export default Home;

const Title = styled.h1`
    color: #b31d15;
`